{{> navbar}}
<h1>Your Cart</h1>
{{#if logged_in}}
    {{#if cartData.length}} 
    <p> cartData is the right name</p>
        <ul>
            {{#each items}}
                <li>
                    {{this.name}} - ${{this.price}}
                    <!-- Add other item details here -->
                </li>
            {{/each}}
        </ul>
    {{else}}
        <p>Your cart is empty.</p>
    {{/if}}
{{else}}
    <p>Please log in to view your cart</p>
{{/if}}

<script src="/js/cart.js"></script>